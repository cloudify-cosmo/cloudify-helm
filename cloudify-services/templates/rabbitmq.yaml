apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: rabbitmq
  name: rabbitmq
spec:
  replicas: {{ .Values.rabbitmq.replicas }}
  selector:
    matchLabels:
      app: rabbitmq
  template:
    metadata:
      labels:
        app: rabbitmq
    spec:
      containers:
        - image: {{ .Values.rabbitmq.image }}
          imagePullPolicy: {{ .Values.rabbitmq.imagePullPolicy }}
          name: rabbitmq
          ports:
            {{- range .Values.rabbitmq.port }}
            - containerPort: {{ . }}
            {{- end }}
          volumeMounts:
            - mountPath: /etc/cloudify/ssl
              name: ssl
              readOnly: true
      restartPolicy: Always
      volumes:
        - name: ssl
          secret:
            secretName: {{ template "cloudify-services.name" . }}-certs
            optional: false

---
apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
spec:
  ports:
    {{- range .Values.rabbitmq.port }}
    - name: {{ . | quote }}
      port: {{ . }}
    {{- end }}
  selector:
    app: rabbitmq
  type: ClusterIP
